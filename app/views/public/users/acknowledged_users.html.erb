<h2>承認したユーザー</h2>

<% if @post_work.acknowledged_users.present? %>
  <% @post_work.acknowledged_users.each do |acknowledged_user| %>
    <p><%= link_to "#{acknowledged_user.name}", user_path(acknowledged_user.id) %></p>
  <% end %>
<% else %>
  <h3>承認したユーザーはいません</h3>
<% end %>